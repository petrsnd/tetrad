<UserControl x:Class="WPF.MultiplayerTypeSelection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Transparent" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Foreground="White" HorizontalAlignment="Left" FontSize="50" Margin="25">Choose Difficulty</TextBlock>
        <Grid Background="Transparent" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <Button Grid.Row="0" Margin="0,0,0,10">
                <TextBlock>Facebook Invite</TextBlock>
            </Button>
            
            <Button Grid.Row="2" Margin="0,0,0,10" >
                <TextBlock>Send Email Invite</TextBlock>
            </Button>
        </Grid>
    </Grid>
</UserControl>
